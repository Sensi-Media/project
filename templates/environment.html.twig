{
    "web": {
        "db": {
            "name": "{{ database }}",
            "user": "{{ user }}",
            "pass": "{{ pass }}"
        }
    },
    "cli": {
        "db": {
            "name": "{{ database }}",
            "user": "{{ user }}",
            "pass": "{{ pass }}"
        }
    },
    "test": {
        "db": {
            "name": "<% current_user %>_{{ project }}"
        }
    },
    "prod": {
        "host": "{{ host }}"
    },
    "dev": {
        "email": "<% user %>@sensimedia.nl",
        "host": "http://<% user %>.{{ project }}.dev.sensimedia.nl",
        "lrport": 8304
    }
}
